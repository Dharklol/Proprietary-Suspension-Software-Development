# Measurement Contract Test Fixtures

The unit tests generate temporary package files at runtime rather than retaining a second example dataset here. This avoids maintaining duplicate CSV examples that can drift from the versioned templates and contract.
