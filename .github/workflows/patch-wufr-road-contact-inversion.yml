name: Patch WUFR road-contact inversion precision

on:
  push:
    branches: [phase-2-wufr-rigid-contact-implementation]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase-2-wufr-rigid-contact-implementation
      - name: Tighten physical wheel-state inversion for derivative work
        shell: python
        run: |
          from pathlib import Path
          path = Path("src/pssd_vehicle/wufr_road_contact.py")
          text = path.read_text(encoding="utf-8")
          old = '''            q_L_tolerance_rad=2.0e-10,\n            displacement_tolerance_m=1.0e-10,\n'''
          new = '''            q_L_tolerance_rad=2.0e-12,\n            displacement_tolerance_m=1.0e-12,\n'''
          if text.count(old) != 1:
              raise SystemExit(f"expected one physical-state tolerance block, found {text.count(old)}")
          path.write_text(text.replace(old, new), encoding="utf-8")
      - name: Commit patch and remove temporary workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/patch-wufr-road-contact-inversion.yml
          git add src/pssd_vehicle/wufr_road_contact.py
          git commit -m "Tighten wheel-state inversion for contact derivatives"
          git push origin HEAD:phase-2-wufr-rigid-contact-implementation
