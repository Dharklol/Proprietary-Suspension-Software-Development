name: WUFR static equilibrium diagnostic retired

on:
  pull_request:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Diagnostic retired after unsprung-gravity reduction audit."
